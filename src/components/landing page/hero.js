import React from "react";

const Hero = () => {
    return(
        <div className="hero-section">
            <h1>Event booking & management made easy</h1>
            <button>Get Started</button>
        </div>
    );
}

export default Hero;